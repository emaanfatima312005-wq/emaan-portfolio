---
kind: external_dependency
name: GSAP Scroll-Driven Animation Engine
slug: gsap-scrolltrigger
category: external_dependency
category_hints:
    - framework_behavior
scope:
    - '**'
---

GSAP 3 with ScrollTrigger (via `@gsap/react`) drives the scroll choreography: camera movement through the room, avatar walk/sit transitions, monitor dive, and fade-in/out of story sections across a 700vh scroll track. Used together with Lenis for smooth scrolling.